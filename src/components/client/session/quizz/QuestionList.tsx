import { question } from '@/components/types'
import React, { useState } from 'react'
import QuestionCard from './QuestionCard';

export default function QuestionList(params:{questions:question[]}) {
  

  return (
    <div>

      {
         question = {questions[currentQuestion]} setCurrentQuestion = {setCurrentQuestion}  />
      }


    </div>
  )
}
